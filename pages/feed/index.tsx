import { FC } from 'react';

import {
    Page,
    ToolBar,
} from '@UI/components';

const Feed: FC = () => {

    return (
        <Page>
            <ToolBar title={'Feed'} />
        </Page>
    );
};

export default Feed;
