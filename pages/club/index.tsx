import { FC } from 'react';

import {
    Page,
    ToolBar,
} from '@UI/components';

const Club: FC = () => {

    return (
        <Page>
            <ToolBar title={'Klub'} />
        </Page>
    );
};

export default Club;
