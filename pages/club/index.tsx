import { FC } from 'react';

import { Page } from '@UI/components';

const Club: FC = ({ children }) => {

    return (
        <Page>
            {children}
        </Page>
    );
};

export default Club;
