import { FC } from 'react';

import { Page } from '@UI/components';

const Radio: FC = ({ children }) => {

    return (
        <Page>
            {children}
        </Page>
    );
};

export default Radio;
