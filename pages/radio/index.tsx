import { FC } from 'react';

import {
    Page,
    ToolBar,
} from '@UI/components';

const Radio: FC = () => {

    return (
        <Page>
            <ToolBar title={'Radio'} />
        </Page>
    );
};

export default Radio;
