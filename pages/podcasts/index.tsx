import { FC } from 'react';

import {
    Page,
    ToolBar,
} from '@UI/components';

const Podcasts: FC = () => {

    return (
        <Page>
            <ToolBar title={'Podcasts'} />
        </Page>
    );
};

export default Podcasts;
